```
npm install --save-dev @vitejs/plugin-basic-ssl
npm install ws form-data

npx nuxi@latest module add tailwindcss
npm i fastify @types/ws uuid @types/uuid
npm install lucide-vue-next

rm -rf certs
mkdir -p certs
cd certs
mkcert -key-file key.pem -cert-file cert.pem localhost 127.0.0.1 192.168.1.159 192.168.1.154 192.168.1.150
cd ..

mkcert localhost 127.0.0.1 192.168.1.159
```
